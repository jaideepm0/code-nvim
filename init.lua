-- Root init.lua for code-nvim plugin
-- This file can be used for automatic initialization (optional)

local vscode_style = require('vscode_style')

-- Note: setup() is intentionally NOT called here
-- Users should call setup() in their config to customize settings
-- Default: require('vscode_style').setup() or require('code-nvim').setup()

return vscode_style
